# Server Configuration
PORT=3001

# Email Configuration (Gmail)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
# For Gmail, use App Password: https://support.google.com/accounts/answer/185833

# Instamojo Payment Gateway Configuration
INSTAMOJO_API_KEY=your_instamojo_api_key
INSTAMOJO_AUTH_TOKEN=your_instamojo_auth_token
INSTAMOJO_ENV=test
# Options: test (for testing) or production (for live payments)
# Get your API credentials from: https://www.instamojo.com/developers/

# Frontend and Backend URLs
FRONTEND_URL=https://ruatfly.github.io/Tlangau
BACKEND_URL=https://your-backend-url.com
# For local development, use: http://localhost:3001
# IMPORTANT: For webhooks to work, BACKEND_URL must be publicly accessible (use ngrok for local testing)

# Admin Dashboard Configuration
ADMIN_PASSWORD=your_secure_admin_password
# This password is used to access the admin dashboard at /admin.html
# IMPORTANT: Use a strong, unique password for production

# Firebase Admin SDK Configuration (for Bundle/Topic Management)
# Option 1: Service account JSON as environment variable (recommended for Railway)
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}
# Option 2: Path to service account JSON file (for local development)
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account-key.json
# Firebase Realtime Database URL
FIREBASE_DATABASE_URL=https://tlangau-123-default-rtdb.asia-southeast1.firebasedatabase.app
# To get service account key:
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate new private key"
# 3. Download the JSON file
# 4. For Railway: Copy the entire JSON content and paste as FIREBASE_SERVICE_ACCOUNT_JSON
# 5. For local: Save the file as service-account-key.json in tlangau-web directory